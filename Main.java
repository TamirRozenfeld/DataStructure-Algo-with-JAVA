import java.awt.Color;

import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class Main {

	public static void main(String[] args) {
	    /*
	        JFrame frame = new JFrame(); //create a frame
	        frame.setTitle("JFrame title goes here"); //sets title of frame
	        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);// exit out of application
	        frame.setResizable(false); //prevent frame from being resize
	        frame.setSize(420, 420); // sets the x and y dimension of frame
	        frame.setVisible(true); //make frame visible
	    
	        ImageIcon image = new ImageIcon("C:/Users/tamir/OneDrive/Desktop/Java projects/MyFirstProgram/src/metal-sign-logo-mockup_1389-1345.jpg"); //create an ImageIcon
	        frame.setIconImage(image.getImage()); //change icon of frame
	        frame.getContentPane().setBackground(new Color(255, 255, 200)); //change color of background
       */
	    
	    new MyFrame();
	}
}
